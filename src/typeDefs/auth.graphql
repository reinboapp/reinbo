type Auth {
  accessToken: String
  refreshToken: String
}

input AuthInput {
  username: String
  email: String
  password: String!
}

input RefreshTokenInput {
  refreshToken: String!
}

type Mutation {
  authLogin(input: AuthInput!): Auth!
  authRefreshToken(input: RefreshTokenInput!): Auth!
}
